# Enterprise Modeling Foundation

This fixture exercises the canonical DataLex modeling primitives used by the
enterprise modeling workflow:

- conceptual entities without forced columns
- model-level relationships
- logical data type mappings
- physical entities with dialect-specific SQL-ready metadata
- semantic model overlays linked to modeled entities
- conceptual, logical, and physical diagram files

It is intentionally small so tests and UI smoke checks can load it quickly.

